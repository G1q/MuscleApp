import styles from './UserProfile.module.css'

const UserWorkouts = () => {
	return (
		<main>
			<h1 className={styles.profileTitle}>My Workouts / Saved Workouts</h1>
		</main>
	)
}

export default UserWorkouts
